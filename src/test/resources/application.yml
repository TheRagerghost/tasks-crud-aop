spring:
  application:
    name: tasks-crud-aop
  profiles:
    include:
      - kafka
      - mail
  datasource:
    url:
    username:
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true